## 0.0.2

* Updated repository links to `Coderkube-App/flutter_flaver_dotenv`.

## 0.0.1

* Initial release.
* Multi-flavor support (`dev`, `staging`, `prod`) via `AppEnv`.
* `.env` asset loading and parsing with `EnvLoader`.
* Startup validation of required keys with `EnvValidator`.
* Type-safe singleton access via `AppConfig`.
* Example app with flavor entry points and sample `.env` files.
